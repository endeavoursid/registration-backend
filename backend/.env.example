# Server
PORT=5000
NODE_ENV=production

# Database (example – adjust if needed)
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=StrongPasswordHere
DB_NAME=autozone

# CORS
FRONTEND_URL=https://yourdomain.com
